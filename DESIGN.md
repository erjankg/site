# DESIGN.md — Wild Rift Stats (PRO-WILDRIFT)

Источник правды по дизайну. Читать ПЕРЕД любой UI-правкой. Не отклоняться без «ок» Эржана.

## Продукт
Справочник/аналитика Wild Rift (статы, винрейт, драфтер, карта, предметы, руны, калькулятор,
драконы, кэмпы). Был популярен и полезен. Аудитория — игроки WR. SPA, ПК-first, тёмная тема.
**Север-звезда: УДОБНО + всё в одном + консистентно/читабельно.**

## Эстетика
**ФУЛЛ-СТЕКЛО (glassmorphism, как iOS). ВСЁ = стекло**, размывает ОДИН глобальный сплэш-арт
за собой. Это и есть лицо сайта — НЕ «цветная тема», НЕ esports-скин. Типографика сплошным
цветом (градиент только в фоне, не в тексте).

## ★ ЯДРО: единая система через ТОКЕНЫ (меняешь токен → меняется весь сайт)
**★★★ ИСТОЧНИК ПРАВДЫ ПО ЧИСЛАМ = `canon-tokens.css` в корне репо** (утверждён в `lab-tokens`
2026-08-01: 156 токенов, 10 категорий). Лабы и боевой ПОДКЛЮЧАЮТ его первой строкой и своих
блоков токенов НЕ держат. Числа ниже дублируются здесь только для чтения — при расхождении
прав `canon-tokens.css`.

**★★ ФИНАЛЬНЫЙ КАНОН СТЕКЛА (выбор Эржана в `lab-tokens`, 2026-08-01, подобран на светлом арте Lux).**
```css
html[data-glass="on"] {
  --glass-op: 0.00;    /* ⚠️ БЕЛОГО ТИНТА НЕТ — только тёмный слой */
  --glass-blur: 9.5px; /* обновлено 2026-08-01 (было 8px) */
  --glass-dark: 0.56;  /* обновлено 2026-08-01 (было 0.46) */
  --glass-dark-strong: 0.61;
  --glass-sat: 1.6;
  --ds-r: 15px;        /* = алиас на --r-lg */
  --grain: 0;          /* зерно ВЫКЛ */
  --sh-k: 1.00;        /* сила тени */
  --bg-blur: 0px;      /* фон НЕ блюрим */
}
```
- **★ СТИЛЬ СТЕКЛА = B «стекло-на-стекле ТЕМНЕЕ»** (выбор 2026-08-01). Вложенная поверхность внутри
  стекла не светлеет белым, а ТЕМНЕЕТ (`--g-inner`/`--g-inner-2`). Второго `backdrop-filter` при этом
  НЕТ — темнота даётся скримом, запрет «стекло-в-стекле» соблюдён. Отвергнутые варианты живут
  в `lab-tokens`: A матовое (прежний канон), C тонировка «авто-стекло» (цветная плёнка, слабый блюр).
- **Материал:** блик ВЫКЛ · **внутренняя тень ВКЛ** · **светлая грань 1px ВКЛ** · ховер = ЯРЧЕ (не цвет) ·
  текст-скрим ВЫКЛ · параллакс ВЫКЛ · Ken Burns ВЫКЛ · **виньетка ВКЛ**
- **★ СТЕКЛО НАД СТЕКЛОМ = режим C: ЯРКОСТЬ + ТЕНЬ** (blur ОДИНАКОВЫЙ у всех), **3 УРОВНЯ ВЫСОТЫ**.
  Не «второй blur», а более светлая поверхность + сильнее тень.
- `--txt:#ffffff` + тень · `--accent:#ffffff` (ТОЛЬКО на данных) · `--anim-dur:.30s` · `--rail-w:235px` · `--pill-size:17px`
- **★★ БЕЛАЯ ШКАЛА ВЫДЕЛЕНИЯ `--sel-*` (канон 2026-07-28, чтобы «белое» перестало быть на глаз).**
  Выделение/ховер/активное/рамки = БЕЛОЕ по этой шкале, НЕ акцент, НЕ синее. Не изобретать свой rgba.
```css
  --sel-surf:   rgba(255,255,255,0.06);  /* фон покоя */
  --sel-surf-2: rgba(255,255,255,0.09);  /* фон приподнятый */
  --sel-hover:  rgba(255,255,255,0.12);  /* ховер */
  --sel-active: rgba(255,255,255,0.18);  /* активное/выбранное */
  --sel-brd:    rgba(255,255,255,0.12);  /* грань */
  --sel-brd-2:  rgba(255,255,255,0.18);  /* грань акцентнее */
  --sel-brd-act:rgba(255,255,255,0.45);  /* грань активного */
  --sel-line:   rgba(255,255,255,0.06);  /* разделитель */
```
  Акцент = БЕЛЫЙ `#ffffff`, как и chrome. Цветными остаются только сами ДАННЫЕ (WR-зелёный/красный, тиры, урон, статы).
- **★★ НЕТ ЭМОДЗИ КАК ИКОНОК ИНТЕРФЕЙСА (Эржан 2026-07-29).** Эмодзи рисуются стилем ОС (Segoe/Microsoft) —
  чужеродно, «майкрософтовский вид», не игрово. Навигация/разделы = ОДИН согласованный набор простых
  ЛИНЕЙНЫХ иконок по канону. Настоящая WR-арт (роли `image/role_*`, статы `siteIcons`, предметы/чемпы/руны) —
  в КОНТЕНТЕ, не в нав-глифе. Где реального WR-ассета нет (концепт сайта: Статс/Тир/Чат…) — линейный глиф,
  НИКОГДА эмодзи. *Почему:* эмодзи = тот самый вид, который бесит владельца.
- **★ ЛИПКОЕ — ДВА ВИДА (Эржан 2026-07-29):**
  (а) **Полностью фиксированное** (пикер справа от таблицы) — при скролле НЕ двигается вообще, всегда на месте.
  (б) **Прилипающее при скролле** (шапка таблицы) — скролится с контентом, но у верха фиксируется с зазором
  **~15px** (`position:sticky; top:15px`), не впритык 0. ГОЧА: sticky ломает `overflow:hidden/auto` у родителя
  и `transform` у элемента.
- **Рельс = Оверлей** по умолчанию, чисто hover.
- ⚙ юзер: выбор сплэш-арта. (Акцент — белый без вариантов; сила/тёмность/blur зафиксированы, из ⚙ убраны.)

Правила:
- Любой видимый блок = `class="glass"` + токены. Своё `background`/`backdrop-filter` ЗАПРЕЩЕНО.
- ОДИН слой стекла в стеке (не вкладывать стекло в стекло → мутнеет «зелёным»).
- Ни один блок не исключение (шапки, вкладки, ячейки).

## Читабельность над сплэшем
**Текст = ЧИСТЫЙ БЕЛЫЙ `#ffffff`** (НЕ серый, НЕ приглушённый) + тень `0 2px 14px rgba(1,10,19,.8)`.
Стекло с тёмным слоем `--glass-dark:.46` (подобрано на светлом арте Lux — худший случай).
НЕ ячейка под каждое слово — достаточно тёмное стекло + белый текст + тень.
**Правило проверки:** любая новая читабельность валидируется на СВЕТЛОМ арте (Lux/Soraka), не на тёмном.

## Единая обёртка + анимация
ОДИН компонент-обёртка (карточка/панель/раздел) — ВСЕ фичи через него. ОДНА анимация
открытия/закрытия (токен, или мгновенно), одинаковая везде, БЕЗ вспышки фона при переключении.
GSAP на боевом для сложного; базовое открытие = один общий переход.

## ★ ПРОИЗВОДИТЕЛЬНОСТЬ СТЕКЛА (лаг убираем БЕЗ потери красоты — стекло НЕ гасим)
Корень лага = анимация РАЗМЕРА (flex/width) → reflow + пересчёт blur у всех стёкол каждый кадр.
Лечим reflow, а НЕ гасим стекло (гашение = колхоз, запрещено как дефолт).

**🔑 ГЛАВНАЯ GOTCHA (корень «стекло пропадает при анимации»):** `backdrop-filter` НЕ рендерится,
если на ТОМ ЖЕ элементе есть `transform`, `will-change: transform/opacity`, `filter` или
`contain: paint`. Браузер отключает стекло. Поэтому:
- Стеклянный блок анимируем **ТОЛЬКО `opacity`** (fade), БЕЗ `translate`/`scale` → стекло не пропадает.
- **`opacity<1` на ПРЕДКЕ стекла ТОЖЕ гасит его** (предок становится backdrop-root). Фейд-opacity
  вешать на САМ `.glass`-блок, НЕ на его контейнер/обёртку вкладок. (Частый баг при смене вкладок.)
- `transform`/`will-change`/`contain:paint` вешаем на **НЕ-стеклянную обёртку** (внешний div),
  а стекло — на внутренний, который НЕ трансформируется.
- Никогда не ставить transform+backdrop-filter на один элемент.
- **Рельс двигать через `transform` (GPU), НЕ через flex/width.** GPU-сдвиг не вызывает reflow →
  blur не пересчитывается → стекло остаётся видимым и плавным. Контент справа сдвигать тоже
  трансформом/через фикс-колонки, не layout-анимацией.
- **Панели/разделы: стекло НЕ гасить.** Их анимация дешёвая (opacity+translate одного блока) —
  стекло видно всё время. НИКАКОГО blanket `backdrop-filter:none` во время анимаций.
- **Надписи рельса — fade по `opacity`** (всегда в DOM), не появляться рывком при раскрытии.
- `contain: layout` (НЕ paint! paint гасит стекло) на разделах — изолирует reflow. `will-change` только на время перехода.
- **Блюр НЕ снижаем даже на миг** (Эржан отверг — выглядит как «стекло слабеет»). Полный blur всегда.
  Лаг лечим иначе: `contain: layout`, короче `--anim-dur`, snap (мгновенно без перехода), или
  GPU-дешёвое поведение (сдвиг-transform / оверлей). Ленивая загрузка = меньше живых стёкол = меньше лага.
  Крайний случай (не сейчас): заранее размытая копия статичного сплэша как фон панели вместо живого
  backdrop-filter (но ломает живую ⚙-регулировку блюра).
- **★ СТЕКЛО ТОЛЬКО НА КОНТЕЙНЕРАХ, не на элементах списка.** Шапка / таблица / пикер / сетка =
  стекло (единицы на вид). **Строки таблицы, карточки чемпов, плитки предметов — БЕЗ
  `backdrop-filter`**, обычный rgba-фон. Иначе 137 строк × backdrop-filter = гарантированный лаг
  (и это же = «один слой стекла», не мутнеет).
- **★★ ТАБЛИЦЫ (Статс / WinRate) — БЕЗ ВНУТРЕННЕГО СКРОЛЛА. НИКОГДА.** Таблица существует ЦЕЛИКОМ,
  скролится **СТРАНИЦА** (как на всех стат-сайтах). Вложенный скролл внутри таблицы = ЗАПРЕЩЁН
  (`overflow-y:auto` / `max-height` на таблице — нельзя). *Внутренний тонкий скролл — только для
  панелей / инструментов / попапов*, которым надо вписаться в экран, но НЕ для таблиц.
- **★★ ШАПКА ТАБЛИЦЫ — НЕ отдельный тёмный блок с острыми углами.** (Эржан ГОДАМИ от этого
  избавлялся, старые Клоды постоянно так делали.) Шапка = ЧАСТЬ той же стеклянной ячейки таблицы
  (общий радиус контейнера, скругление сверху, НЕ острые углы, НЕ своя тёмная плашка во всю ширину).
  Никакого захардкоженного тёмного/синего фона у `thead`. `.glass--strong` на шапку таблицы — НЕЛЬЗЯ
  (создаёт ровно тот ненавистный блок). Заголовки колонок читаемы за счёт белого текста, а не тёмной плашки.

## Цвет
Стекло белое (`--glass-tint:255,255,255`). За стеклом — глобальный сплэш-арт (не градиент).

**★★ ВЫДЕЛЕНИЕ/ХОВЕР КНОПОК = БЕЛОЕ, НЕ АКЦЕНТ.** (Эржан 2026-07-13: «почему все лабы синие?»)
- Активная/выбранная кнопка, ховер, сегменты, вкладки = **ярче БЕЛОГО стекла** (напр. `rgba(255,255,255,.14→.20)`),
  БЕЗ цветной рамки-кольца. **Акцент-рамка/ring на кнопках — ЗАПРЕЩЕНО** (это была отсебятина Клода).
- **Акцент используется ТОЛЬКО для ДАННЫХ:** тиры (S/A/B/C), винрейт-цвета, важные числа, ↑↓ движение меты.
  НЕ для покраски интерфейса.
- **★★ АКЦЕНТ = БЕЛЫЙ `#ffffff`. ОДИН, БЕЗ ВАРИАНТОВ (Эржан 2026-08-07).** Циан `#0BC4E3` и золото
  как варианты акцента УДАЛЕНЫ из кода, документов и памяти — циан был выбран раньше ТОЛЬКО
  потому, что белого не было в списке. Выбора акцента в ⚙ БОЛЬШЕ НЕТ, не возвращать.
  Цвет на сайте несут ТОЛЬКО данные: WR (зелёный/красный), тиры, типы урона, статы, роли,
  статусы, валюта `--gold` (это цена предметов, а не акцент).
  Меняет только цвет ДАННЫХ, не трогает кнопки/стекло.

## Типографика + минимализм (Эржан: «имба» — ДЕРЖАТЬ)
Стиль из `lab-design-system/` = утверждён: минимализм, меньше отступов между
линиями/блоками, тонкие чистые разделители, крупные читаемые шрифты (шрифты лаба),
сплошной цвет. Не раздувать отступы, не лепить лишние рамки/линии. Плотно и чисто.

## Отступы
База 4px. Шкала: 4 · 8 · 16 · 24 · 32 · 48 · 64.

## Навигация (модель Suno)
Рельс ВСЕГДА на месте (простой, иконки, без дёрганья), НО **сворачивается** оверлеем для
фуллскрин-разделов (драфтер, Страта-коучинг): свернул → раздел на весь экран.
- **Home** = главная страница, верхние под-вкладки: **Статс / WinRate / Карта / Метахаб / Тир-лист**. «Карта» ЗАМЕНЯЕТ отдельную Тактич-доску в верхних вкладках.
  - **Карта** внутри = ещё под-вкладки: **[Карта (инфо/экономика)] [Страта (тактич-доска)]** (+ потом добавим).
- **Инструменты** (в рельсе) = разделы-страницы (Драфтер/Калькулятор/Предметы/Руны/Драконы/Кэмпы/**Патч**).
- **Модалок НЕТ** — всё через единую обёртку-раздел. Исключение: кооп-драфт (клетка, фуллскрин, без выхода).
- **ПК — место используем РАЗУМНО, НЕ «фулл-ширина».** Не зажимать контент в узкую колонку — но и
  НЕ растягивать одинокий элемент / 2 слова на весь экран. Тому, чему нужно место (широкие таблицы,
  2-колоночные раскладки), — даём. Короткому контенту — компактно, пустые бока это НОРМА.
  - **★ МЕХАНИЗМ (2026-08-01): обёртка контента носит `class="shell"`** (`canon-tokens.css`) —
    `max-width: var(--content-max)` = 1400px. Пока предел число, растянуть кнопку/панель на весь
    монитор ФИЗИЧЕСКИ невозможно, даже с `width:100%` внутри. **`max-width:none` на обёртке
    контента ЗАПРЕЩЁН** — именно он (`styles.css:7519`) и есть та самая болезнь.

## ★ ОПТИМИЗАЦИЯ (не забывать — сайт должен быть лёгким/быстрым)
- **Разделы = ленивая загрузка (lazy).** НЕ грузить весь JS/контент сразу. Раздел (Карта/
  Калькулятор/Драфтер…) грузится при ПЕРВОМ открытии, не на старте → меньше вес, быстрее.
- Не держать десятки `backdrop-filter` активными разом (см. «ПРОИЗВОДИТЕЛЬНОСТЬ СТЕКЛА»).
- Тяжёлые данные (jungle-economy, WR-статы) — отдельными файлами, подгружать по надобности.

## ★★★ Э1 — ПОЛНАЯ ЗАМЕНА СТАРОГО (ноль остатков)
Цель Э1: **весь старый дизайн/стиль/анимации/хардкод УМИРАЮТ, наше НОВОЕ их полностью заменяет.**
- **Ни одного старого стекла, цвета, анимации, шрифта, хардкод-стиля не остаётся** после Э1.
- ⚠️ Есть куски боевого, по которым ЛАБОВ НЕТ (Эржан не всё пересмотрел). Их ТОЖЕ привести к канону
  (`DESIGN.md`), НЕ оставлять старьё. Нашёл кусок без лаба → флажить Эржану (нужен ли лаб / вариант),
  но старый стиль там всё равно СНЕСТИ и одеть в канон.
- Способ проверки «ничего старого не осталось»: после Э1 прогнать по коду — нет своих `background`/
  `backdrop-filter`/цветов/keyframes мимо токенов канона; всё через `.glass` + `--glass-*` + утв. дефолты.
- Метод: СНОСИМ старую презентацию (не «переодеваем поверх»), собираем из лаба + подключаем логику.

## ★★ UI-KIT — УТВЕРЖДЁННЫЙ ВЫБОР ЭРЖАНА (lab-ui-kit v4, 2026-07-18). Язык компонентов всего сайта.
Все элементы берём ОТСЮДА, не изобретаем свои. Арт-фон при подборе: Lux (светлый = худший случай).
- **Кнопки:** размер M · **ripple** · **lift** (подъём+тень)
- **Крестик/иконки:** эффект **rotate** · фигура **squircle**
- **Табы/сегменты:** **pill** (скользящая пилюля)
- **Чипы-фильтры:** с **count** + с иконкой
- **Тумблеры:** **slide**, размер M · **Ползунки:** трек **fill** + **ticks**
- **Дропдауны:** **visual** (визуальный пикер, не нативный select)
- **Поиск:** с **results** (выпадающие результаты) · **Бейджи:** **fill**
- **Тултипы:** **spring** + **rich** (расширенные)
- **Тосты:** снизу-справа · модель **stack** (Sonner) · стиль **solid**
- **Карточки:** поверхность **spotlight** (свет за курсором) + **lift**
- **Списки/числа:** строки **cards** · **Скелетоны:** **spinner**
- **Аватарки:** **squircle**, M, с **рангом** и **статусом**
- **Пустые состояния:** **results** · **Смена вида:** переход **slide**
- **Overscroll:** **edgefade** · **Прогресс:** **steps** · **KPI:** **spark** · **Рейтинг:** **count**
- ❌ НЕ берём: маркиз-тикер · магнитный dock-рельс · (иконки статов draw-on — уточняется)
- **Правило:** элемент включаем ТОЛЬКО если есть реальное место применения на сайте («если есть куда юзать»).

## ★★ АНИМАЦИИ — уровень топ-сайтов (ресёрч 2026-07-16). Единые токены + слой-сэндвич.
Эффекты уровня Linear/Vercel/Stripe. Токены (весь сайт двигается «одним материалом»):
```css
--ease-out-expo: cubic-bezier(.16,1,.3,1);   /* появления */
--ease-standard: cubic-bezier(.4,0,.2,1);    /* UI-движение, пилюля */
--ease-overshoot: cubic-bezier(.34,1.56,.64,1); /* press/иконки, лёгкий отскок */
--dur-fast:130ms; --dur-base:200ms; --dur-slow:280ms; --stagger:45ms;
```
- **★ СЛОЙ-СЭНДВИЧ (решает «стекло гаснет при анимации»):** обёртка (transform тут) → стеклянный
  слой (СТОИТ, только backdrop-filter) → fx-слой (spotlight/shimmer/glow: background/opacity) → контент.
  Движение НИКОГДА не на стеклянном слое. Безопасные «дорогие» эффекты = background/opacity (spotlight,
  shimmer, glow, gradient-border); transform-эффекты (tilt/magnetic/FLIP) — только на НЕ-стеклянной обёртке.
- Анимировать ТОЛЬКО `transform`/`opacity`. Длительность <300ms, exit на ~20% быстрее входа, крупное дольше мелкого.
- Топ-10 обязательных: spring-press · hover-lift+тень · скользящая пилюля · spotlight-follow · stagger+count-up ·
  тосты-Sonner (стопка+свайп) · крестик rotate+галочка-draw · shimmer на премиум · FLIP-сортировка · единые токены.
- `prefers-reduced-motion` → движение заменять на fade (`gsap.matchMedia()`).

## ★★ ЗНАКОМОСТЬ: игровые фичи ПОВТОРЯЮТ экраны игры WR (Эржан 2026-07-15)
Всё, что связано с механикой игры — **Сборка (loadout) · Руны · Дерево предмета · Драфт** — по
раскладке делать ПОХОЖИМ на соответствующий экран Wild Rift. Игрок узнаёт интерфейс → интуитивно
и психологически понятно, ноль обучения. Эржан присылает скрины из игры как референс — использовать.
- Сборка = как экран LOADOUTS (модель чемпа + ряды Предметы / Руны / Заклинания).
- Руны = как игровой экран рун (ряд кейстоунов сверху + 2 ветки-колонки со слотами + инфо), НЕ строка.
- Дерево предмета = как в игре (из чего собирается; клик по куску → что ещё из него собрать) + контр-предметы.
Материал/стекло — наш канон, но РАСКЛАДКА/логика экрана = как в игре.

**★ ИНФОРМАТИВНЫЙ СКЕЙЛ (Эржан 2026-07-18):** умения и руны показывают урон/эффект как
**база + скейл от стата** (напр. «Урон 200 = 130 база + 70 от Силы умений»; руна Завоеватель:
+3-5 AD за заряд). Число ЖИВО пересчитывается от текущей сборки (купил AP-предмет → умение обновилось).
Видно ОТКУДА урон и ОТ КАКОГО стата (AD/AP/HP). Связывает страницу чемпа ↔ Сборку/симулятор.
Данные умений (база+ratio) — реальные при порте, демо в лабе.

## Decisions Log
| Дата | Решение | Почему |
|------|---------|--------|
| 2026-07-05 | Канон создан (/design-consultation) | зафиксировать единую токен-систему, убить хардкод-стекло |
| 2026-07-05 | Эстетика = фулл-стекло (не esports-тема) | так решил Эржан; цвет только функциональный |
| 2026-07-05 | Навигация = модель Suno (рельс+разделы, сворачивается) | убрать модалки, единая обёртка |
