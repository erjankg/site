# Э1-PLAN.md — миграция боевого на канон (презентация, view-by-view)

**Тип:** план. КОД НЕ ТРОГАЕМ этим документом. Логику/данные НЕ меняем — только презентация.
**Источники:** `DESIGN.md` (канон + утв. дефолты 2026-07-11) · `MASTERPLAN.md` (Э1) · `INVENTORY.md` (аудит боевого) · `lab-design-system/` (эталон-код).
**Дата:** 2026-07-11.

---

## 0. ДЕЛЬТА: боевой vs канон (что именно расходится)

### 0.1 Токены (боевой → канон)
| Токен | Боевой сейчас | Канон (утв.) | Действие |
|---|---|---|---|
| `--glass-op` | `.06` | **`.05`** | подкрутить |
| `--glass-blur` | `10px` | **`9px`** | подкрутить |
| `--glass-dark` | **НЕТ** | **`.26`** | 🔴 **добавить** — тёмный слой в `.glass` (читабельность над сплэшем) |
| `--glass-sat` | `1.6` | `1.6` | ✅ совпало |
| `--glass-bd-c` | `rgba(255,255,255,.12)` | то же | ✅ |
| `--ds-r` | `16px` | `16px` | ✅ |
| `--scrim-bg` | `rgba(2,9,15,.72)` | то же | ✅ |
| `--accent` | `#ffffff` | то же | ✅ |
| `--anim-dur` | нет (разнобой) | **`.30s`** | добавить, свести все переходы к нему |
| `--rail-w` / `--rail-min` | ширина `240px` (меняется) | **`230px` / `64px`** | добавить |
| `--pill-size` | нет | `17px` | добавить |

**Главное:** канонный `.glass` = `linear-gradient(rgba(2,9,15,var(--glass-dark)) ×2) + rgba(var(--glass-tint),var(--glass-op))` + blur/sat + border + radius. У боевого тёмного слоя НЕТ → над сплэшем текст «плавает». Это корень жалоб на читабельность.

### 0.2 Каркас
| Механизм | Боевой | Канон | Действие |
|---|---|---|---|
| Стекло на блок | легаси-список `:is(.lvl-container,.m-win,…)` | `class="glass"` | перевести блоки на `.glass`, список НЕ расширять |
| Оболочка вида | `.m-mask`/`.m-win` (24 модалки в HTML, 222 КБ) | `.section[data-section]` | 🔴 пересобрать |
| Переключение | `switchMainView()` + `_modalStack` | `showSection(name)` | пересобрать |
| Рельс | `#sidePanel`, раскрытие через **width** (reflow → лаг) | `.rail`, раскрытие через **clip-path** (нет reflow) | 🔴 пересобрать |
| Режим рельса | раздвигание | **оверлей (дефолт)**, раздвигание — опция ⚙ | пересобрать |
| Контент-область | `.app-wrapper` + `data-toolopen` (visibility:hidden) | `.stage` (absolute, `left/right`, `contain:layout`) | пересобрать |
| Под-вкладки | `.f-tab` (`#viewTabs`) | `.subtab` + `.tabpane` | переиспользовать механику эталона |
| Анимация | разнобой (`modalRiseBlurIn`, morph, CRT — часть уже убрана) | **ОДНА:** `fadeIn` по `opacity` **на самом `.glass`** | свести к одной |
| Тактич-доска | `location.href='tactics-board/'` (уход со страницы) | инлайн-раздел «Страта» | Э3, слот в Э1 |

### 0.3 🔑 GOTCHA канона (жёстко соблюдать в каждом под-этапе)
`backdrop-filter` **не рендерится**, если на ТОМ ЖЕ элементе есть `transform` / `will-change: transform|opacity` / `filter` / `contain: paint`. И `opacity<1` **на предке** стекла тоже его гасит.
→ Стекло анимируем **только `opacity`, на самом `.glass`-блоке**. `transform`/`contain:paint` — только на НЕ-стеклянной обёртке. `contain: layout` — можно. **Блюр не снижаем ни на миг.**

---

## 1. ЦЕЛЕВОЙ КАРКАС (из эталона, 1-в-1)

```
.app  [mode-overlay | mode-shift] [rail-open]
├── .rail.glass                    ← ОДИН стеклянный элемент, clip-path сворачивает
│   └── .rail-items > .rail-btn[data-section]   (+ .rail-gear ⚙ внизу)
└── .stage                          ← absolute, left/right, contain: layout
    └── .section[data-section]      ← ЕДИНАЯ обёртка раздела (display:none/block, overflow-y:auto)
        ├── (Home) .subtabs > .subtab[data-tab]
        └── .tabpane[data-tab]
            └── .glass-ячейки контента
```

**Правило раскладки вида:** внутри `.section` — flex-column:
`шапка-ячейка (flex:0 0 auto)` + `контент-ячейка (flex:1; min-height:0; overflow-y:auto)`.
Так шапка не уезжает, длинный контент скроллится ВНУТРИ своей ячейки (см. §4).

---

## 2. ПОРЯДОК МИГРАЦИИ (под-этапы Э1.x)

Стратегия — **strangler**: новый каркас вводим рядом, виды переносим по одному, старую модалку сносим последней. Сайт остаётся рабочим на каждом шаге.

| # | Под-этап | Что | Почему такой порядок |
|---|---|---|---|
| **Э1.0** | **Токены** | Привести `--glass-*` к утв. дефолтам + добавить `--glass-dark` в `.glass`. Структуру НЕ трогаем. | Самый дешёвый и самый заметный шаг: весь сайт разом получает канонный материал + читабельность. Риск ~0 (только значения). |
| **Э1.1** | **Каркас** | Ввести `.app/.rail/.stage/.section`. Рельс → clip-path + **оверлей**. `switchMainView` → `showSection`. Виды пока **перепарентить как есть** (контент не переписываем). | Чинит лаг рельса (reflow) и даёт скелет. Ничего визуально не ломает — контент тот же, просто в новой оболочке. |
| **Э1.2** | 🚩 **ПИЛОТ: WinRate** | Первый вид, одетый ПОЛНОСТЬЮ в канон: `.glass`-ячейки, токены, скролл-контейнер под объём. | **Самый изолированный:** уже инлайн (не модалка, `#wrprSection`), свои фильтры + своя таблица, реальные данные (`wr-stats.json`), не дефолтный вид → если сломается, главный экран жив. Даёт эталон для остальных. |
| **Э1.3** | **Статс** | 3 ячейки: Уровень · Таблица · Пикер. | Самый объёмный (137 чемпов, колонки, ховер) → делаем ПОСЛЕ того, как пилот доказал паттерн. Модель «3 раздельные панели» на боевом уже есть → переиспользуем. |
| **Э1.4** | **Тир-лист** | Модалка → инлайн-`.section`. | Средняя сложность; закрывает долг `feedback_top_tabs_contract` (сейчас фуллскрин-модалка). |
| **Э1.5** | **Метахаб** | Веер-витрину сохранить **1-в-1** (`feedback_keep_godtier`), одеть контейнер в канон. | Веер god-tier — трогать только обёртку, не сам веер. |
| **Э1.6** | **Инструменты рельса** | Предметы · Руны · Чемпионы · Калькулятор · Чат · Киберспорт · Изменения → `.section` + **lazy**. | Их контент уже разложен на стеклянные ячейки (см. §5) → переносим декомпозицию, меняем оболочку. |
| **Э1.7** | **Драфтер** | Solo → `.section`. Кооп → **клетка, фуллскрин** (исключение канона, `data-no-esc`). | Граница: кооп НЕ инлайним. |
| **Э1.8** | **Карта ✅ (не слот, а сразу контент)** | Вкладка «Тактич» → **«Карта»** с инлайн под-вкладками [Карта (экономика)][Страта (доска)]. Порт из `lab-map`, доска — из `tactics-board` без потерь. Драконы/Кэмпы/Патч — слоты остаются на потом. | Лаб был готов, поэтому вместо пустой заглушки сразу налили Э2+Э3. Сделано 2026-08-21. |
| **Э1.9** | **Уборка/вес** | Снести `.m-mask`/`_modalStack`/24 инлайн-модалки; ужать `index.html` (222 КБ); `cms.js` → `cms-data` + `cms-admin` (lazy); убрать `draft.js`/`cybersport.js` из SW precache. | Только когда все виды переехали. Это и есть выигрыш по весу. |

**Гейт на каждом под-этапе:** Г1 (разведка) → правка → Г3 (браузер: скрин + нет мерцания + стекло не гаснет при анимации + рельс не лагает) → бамп SW.

---

## 3. РЕЗЕРВ СЛОТОВ ПОД БУДУЩЕЕ (закладываем в Э1.8, до наполнения)

### 3.1 Home — верхние под-вкладки (5 штук, порядок канона)
`Статс · WinRate · **Карта** · Метахаб · Тир-лист`
- **Карта** добавляем СРАЗУ как `.tabpane[data-tab="map"]` — пустая, с **вложенными под-вкладками внутри**:
  - `[Карта (инфо/экономика)]` → `.subtab[data-tab="map-eco"]` → Э2
  - `[Страта (тактич-доска)]` → `.subtab[data-tab="map-strat"]` → Э3
  - Механика вложенных под-вкладок = та же `.subtab`/`.tabpane` из эталона (второй уровень) → **новый уровень строить не надо**.
- «Тактич» из верхних вкладок **убирается** (Карта её заменяет); `location.href='tactics-board/'` умирает в Э3.
- Данные для Карты уже лежат: `data-pipeline/jungle-economy.json`.

### 3.2 Рельс — кнопки-разделы (слоты заранее)
`Home · Драфтер · Калькулятор · Предметы · Руны · **Драконы** · **Кэмпы** · **Патч** · Чат · Киберспорт · ⚙`
- **Драконы**, **Кэмпы** (Э5) и **Патч** (сейчас плейсхолдер в видах → **переезжает в рельс** по MASTERPLAN) заводим как `.rail-btn[data-section]` + пустой `.section` с надписью «скоро».
- Каждый слот = **lazy**: `.section` пустой, контент грузится при первом открытии → пустые слоты ничего не весят.

### 3.3 Почему слоты сейчас, а не потом
Канон говорит «этап знает будущие и резервирует место». Если рельс/под-вкладки собрать без слотов, Э2/Э3/Э5 придётся ЛОМАТЬ навигацию (порядок кнопок, ширина, активные состояния). Пустые слоты стоят ~0 и убирают этот риск.

---

## 4. РЕАЛЬНЫЙ КОНТЕНТ vs ДЕМО — как контейнер держит объём

Боевой ≠ лаб: 137 чемпов, ~100 предметов, ~55 рун, длинные таблицы. Лаб на демо-данных этого не проверял. Правила, чтобы не сломалось:

### 4.1 Скролл — ВНУТРИ ячейки, не у страницы
- `.section { height:100%; overflow-y:auto; contain: layout; }` — общий скролл раздела.
- Но для видов с длинным списком — **flex-column**: шапка-ячейка `flex:0 0 auto` + контент-ячейка `flex:1; min-height:0; overflow-y:auto`.
- `min-height:0` — обязательно, иначе flex-ребёнок не сожмётся и вылезет (классический баг).
- Скролл — **тонкий/стилизованный** (`feedback_no_ugly_scrollbar`), жирный дефолтный ползунок запрещён.

### 4.2 🔴 ГЛАВНОЕ ПРАВИЛО ПРОИЗВОДИТЕЛЬНОСТИ: стекло — только на КОНТЕЙНЕРАХ
DESIGN.md: «не держать десятки `backdrop-filter` активными разом».
- `.glass` вешаем на **ячейку-контейнер** (шапка, таблица, пикер, сетка категории) — их **единицы** на вид.
- **Строки таблицы / карточки чемпов / плитки предметов — БЕЗ `backdrop-filter`**, просто полупрозрачный `rgba(...)`-фон.
- Иначе 137 строк × backdrop-filter = гарантированный лаг.
- Бонус: это же и «ОДИН слой стекла» (не вкладывать стекло в стекло → не мутнеет).

### 4.3 Виртуализация/пагинация — НЕ нужна (пока)
137 / 100 / 55 элементов — это сотни DOM-узлов, не тысячи. Обычный grid + внутренний скролл справляется. **Виртуализацию не вводим** (лишняя сложность). Пересмотреть, только если появится список 1000+.

### 4.4 Фикс-габариты (без дёрганья)
Переключение фильтров/вкладок **не меняет размер/положение** контейнера (`feedback_no_layout_jerk`): габариты ячейки фиксированы, меняется только её внутренний контент + скролл.

### 4.5 Ленивая загрузка
Раздел грузится при ПЕРВОМ открытии (`DESIGN.md` §Оптимизация). Меньше живых стёкол одновременно = меньше лага + легче старт.

---

## 5. ЧТО ПЕРЕИСПОЛЬЗУЕМ / ЧТО ПЕРЕСОБИРАЕМ

### ✅ Переиспользуем (уже сделано, ложится в канон)
| Актив | Как ложится |
|---|---|
| **Сайдбар-рескин инструментов** (сделан): рамка-окно сквозная, контент разложен на **стеклянные ячейки**, шапка = ячейка со скруглением | 🎯 **Декомпозиция контента переносится 1-в-1** в `.section`. Меняем только оболочку (`.m-mask/.m-win` → `.section`). Это самый ценный задел. |
| **Модель Статов «3 раздельные панели»** (Уровень + Таблица + Пикер) | Уже = канон «раздельные ячейки без общей рамки». Переносим как есть. |
| Токен-система `--glass-*` + ⚙ юзера | Каркас есть — **перенастраиваем значения** (§0.1) + добавляем `--glass-dark`. |
| `#siteSplash` (один глобальный сплэш) | Канон это и требует. Оставляем. |
| Единый `--scrim-bg` + единый blur (сведены) | Совпадает с каноном. |
| Убранная вспышка при переключении (`_instantHideEl`) | Принцип «без вспышки фона» переносим в `showSection` (fade только по `opacity` на `.glass`). |
| Логика/данные: `wrprRender`, `drawM`, `renderMetaHub`, `renderTierBoard`, `_colSettings`, `calc-app/` | **НЕ ТРОГАЕМ.** Меняется только то, куда они рендерят. |
| SEO-статика (~300 лендингов) | Не трогаем. |

### 🔄 Пересобираем (не «лепим поверх»)
| Что | Почему |
|---|---|
| **Рельс**: width-анимация → **clip-path** + оверлей | Корень лага (reflow пересчитывает blur всех стёкол каждый кадр). Лечим reflow, а не гасим стекло. |
| **Модалки** (`.m-mask`/`.m-win`/`_modalStack`, 24 шт в HTML) → `.section` + lazy | Канон: «модалок НЕТ». Плюс это −222 КБ HTML. Исключение: кооп-драфт (клетка). |
| **Легаси-список стекла** `:is(.lvl-container,.m-win,…)` → `class="glass"` | Один источник правды. Список НЕ расширять. |
| **Анимации открытия** (разнобой) → одна `fadeIn` по `opacity` на `.glass` | Канон + GOTCHA (transform гасит стекло). |
| **Тактич-доска** (отд. страница) → инлайн «Страта» | Ломает контракт вкладок. Слот в Э1, наполнение в Э3. |
| **Патч** (вид-плейсхолдер) → раздел в **рельсе** | По MASTERPLAN. |
| `cms.js` (188 КБ на старте) → `cms-data` + `cms-admin` (lazy) | 99% посетителей не админы. |
| SW precache `draft.js`+`cybersport.js` | Отменяет выгоду lazy (~380 КБ на первом визите). |

---

## 6. ВИД ЗА ВИДОМ — как одеть в канон

| Вид | Ячейки (канон) | Объём/контейнер | Особое |
|---|---|---|---|
| **WinRate** 🚩пилот | шапка+фильтры · таблица | таблица: `flex:1; min-height:0; overflow-y:auto`; строки без backdrop | Уже инлайн → чистейший пилот |
| **Статс** | Уровень · Таблица(137) · Пикер | таблица + пикер — каждый со своим внутр. скроллом; строки/карточки без backdrop | Дефолтный вид → мигрируем после пилота |
| **Метахаб** | контейнер-ячейка + веер внутри | веер как есть | Веер **1-в-1**, не трогать |
| **Тир-лист** | шапка · доска тиров | доска: внутр. скролл | Из модалки → инлайн |
| **Карта** (слот) | под-вкладки `[Карта][Страта]` | — | Пустой, Э2/Э3 |
| **Инструменты рельса** | шапка-ячейка + контент-ячейки | сетки (предметы/руны/чемпы) — внутр. скролл, плитки без backdrop | Декомпозиция уже готова |
| **Драфтер** | solo → раздел | — | Кооп = клетка (фуллскрин) |

---

## 7. РИСКИ

1. **Стекло гаснет при анимации** — самый частый провал. Лечится только соблюдением GOTCHA (§0.3). Проверять В БРАУЗЕРЕ на каждом под-этапе.
2. **Дефолтный вид (Статс)** — пока не мигрирован, главный экран должен работать на старой оболочке → strangler-порядок обязателен.
3. **Объём (137 чемпов)** — если по привычке навесить `.glass` на каждую строку → лаг. См. §4.2.
4. **`app.js` = 5+ IIFE-коробок** — приватные `var` не видны между коробками (корень cross-scope `ReferenceError`). Новый `showSection` экспонировать через `window.*`.
5. **Память `project_appjs_iife_scopes` устарела** (говорит про 3 коробки) — обновить перед кодом.

---

## 8. ИТОГ ОДНОЙ СТРОКОЙ
Э1 = **токены → каркас (рельс clip-path + `.section`) → пилот WinRate → Статс → Тир → Метахаб → инструменты → драфтер → слоты (Карта/Драконы/Кэмпы/Патч) → снос модалок и вес**. Логику не трогаем; стекло — только на контейнерах (объём!); рельс лечим reflow, а не гашением стекла.

_Создан 2026-07-11. Обновлять при закрытии под-этапов._
