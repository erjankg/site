# PORT-BACKLOG.md — статус переноса лабов → боевой

Аудит 2026-06-30 (read-only, с пруфами). Что из песочниц `lab-*/` уже на боевом, что нет.
Обновлять при переносе: фича доехала на боевой → статус ✅, строку «осталось» убрать.

## ✅ ПОРТНУТО полностью (6)
- **lab-main** — верхние вкладки (Статы/WinRate/Мета-хаб/Тир/Патч/Тактич), `switchMainView`. *Хвост:* вид «Патч» = плейсхолдер (app.js:6639).
- **lab-champ-picker** — пикер чемпов (#mMask стекло+сплеш+WR-шторка, styles.css:1248). *(багует по #3 — отдельная задача, но портнут.)*
- **lab-item-card** — карточка предмета, 3 вкладки (app.js:1032).
- **lab-hover-reveal** — веер-витрина в Мета-хабе (app.js:7245). *(память metahab_fan устарела — веер УЖЕ на боевом.)*
- **lab-damage-calc** — переехал в `calc-app/` v34 (champ-base.js, реальные статы). ⚠️ Папка lab-damage-calc = stale-копия v18, не путать.
- **lab-chat** — карточки-ссылки 📎 (app.js:5036). *Хвост:* фото-вложения (фаза 2) — позже.

## 🟡 ЧАСТИЧНО (1)
- **lab-drafter** — solo-драфт портнут (app.js:3060). ❌ Coop/фуллскрин-драфтер (draft.js) до дизайна лаба НЕ доведён.

## ✅ ПОРТНУТО (доп., Э1.9 · 2026-08-22)
- **страница чемпа** (источник = `lab-hover-reveal`, не удалённый `lab-champion-card`) — модалка `champDetailMask` снесена, клик по чемпу везде ведёт на СТРАНИЦУ `champions/<slug>/`, объединённую с SEO-страницей: разметку печатает общий `champ-page.js` (Node-генератор + браузер). *Хвосты в бэклог:* видео YouTube/Twitch (нет источника), ★ избранное (нет фичи), спарклайн по патчам (нет истории), перевод описаний умений (есть только EN/CN), сравнение чемпов (пока кнопка ведёт в пикер), карточка-ссылка чемпа «В чат» (зовёт несуществующий `window.chatShareChampion`, пока просто открывает чат).

## 🔴 НЕ ПОРТНУТО (3)
- **lab-hover** — hover-микроанимации карточек на боевом нет. → решить какие тащить, потом порт.
- **lab-profile** — профиль на боевом есть, но panel-based, БЕЗ лабовской «превью-карточки как тебя видят» + сплэш любимого чемпа. → перенести превью-карточку.
- **lab-metahub** — bento-дизайн (rankPills/parallax/hub-art) НЕ перенесён; роль метахаба закрыли lab-main hubView + веер. → РЕШИТЬ: нужен bento или лаб закрыть как исследование.

## 🔧 В ПРОЦЕССЕ (2)
- **lab-sidebar-views** — перевод инструментов рельса в инлайн-вкладки. Образец «Предметы» готов (Task C); остальные инструменты — тем же паттерном.
- **lab-youtube** — блок «видео сильных игроков» под гайдом чемпа на боевом НЕТ; лаб не закоммичен. → перенести блок видео в карточку/страницу чемпа.

_Последнее обновление: 2026-06-30 (аудит Клода №3)._

---

## 🔴 БЛОК ДЛЯ ВСЕХ ПРОМПТОВ — вставлять в КАЖДЫЙ (одобрено 2026-07-20)

Копировать дословно в любой промпт, где Клод трогает UI, переносит фичу из лаба или правит боевой.
Не полагаться на то, что Клод «помнит» — он не помнит.

```
═══ ОБЯЗАТЕЛЬНО: НЕ ТАЩИТЬ ДЁРГАНЬЕ ═══
«Весь экран дёргается при любом нажатии» — ОДНА болезнь по всему старому коду,
скрытых мест сотни. Правило встроено в перенос. НЕ тащи с собой:
1. Анимацию появления на ПОСТОЯННОМ блоке (шапка, таблица, панель, рельс).
   Появление — только модалке, попапу, тосту, НОВОМУ элементу списка.
2. Вечные (infinite) декоративные анимации: пульсация, свечение, бегущий блик,
   градиент по тексту. infinite можно только функциональному: спиннер загрузки,
   «твой ход», непрочитанное (и то ограничить числом повторов).
3. Помни: display:none → обратно ПЕРЕЗАПУСКАЕТ CSS-анимацию. Блок, который
   прячется при смене вкладки, анимацию появления носить НЕ МОЖЕТ.
4. Принудительный перезапуск (void el.offsetWidth между remove/add класса) —
   только единичному элементу, который РЕАЛЬНО появился. Никогда на контейнер.
5. Перерисовку всего ради одного: не innerHTML на весь контейнер
   на каждый клик / букву в поиске / тумблер. Изменилось одно — обнови одно.

ПРИЁМКА СЧЁТЧИКОМ (цифры в отчёт, «вроде не дёргается» не принимается):
пометь узлы, выполни действие, посчитай сколько выжило.
Формат: «197/197 ячеек пережили клик». Здоровое действие меняет ЕДИНИЦЫ узлов.

ГОЧА: transform / will-change / filter / contain УБИВАЮТ backdrop-filter →
трансформ на ОБЁРТКЕ, стекло на отдельном статичном слое.
opacity<1 у ПРЕДКА тоже убивает стекло у потомка.
```

### 🛠 ИНСТРУМЕНТ ПРОТИВ ДЁРГАНЬЯ — `lab-morph.js` (2026-07-29)

Лекарство лежит в коде, не «в голове». Подключается одной строкой перед скриптом лаба:
`<script src="../lab-morph.js?v=2"></script>`.

- `labMorph(host, html)` — вместо `host.innerHTML = html`. Сравнивает новую разметку со старым
  деревом и трогает ТОЛЬКО изменившееся (текст, атрибут, лишний узел). Узлы живут дальше:
  классы, скролл, фокус, играющий плеер — на месте, появление не переигрывается.
- `data-key="имя"` на блоке = якорь. Нужен там, где блок то есть, то нет (закреп, полоса,
  вкладка): без якоря сдвиг соседей рубит живые узлы.
- `labNodeMark(host)` / `labNodeCount()` — счётчик приёмки прямо в консоли браузера.
- ГОЧА: узел теперь переживает ре-рендер → `addEventListener` в коде ре-вайринга вешать
  под флагом (`if (!el.__wired) { el.__wired = 1; … }`). `el.onclick = …` безопасен.
- Что morph НЕ лечит: если раздел реально другой (лента чата → лобби драфта), узлы честно
  меняются. Лечится показом/скрытием готовых узлов (`.is-off` / `hidden`), как сделано
  в пикерах lab-hover-reveal, lab-profile и в каналах lab-chat.

### ✅ БРАУЗЕР РАЗРЕШЁН для самопроверки (Эржан 2026-07-21) — тоже в каждый промпт

```
БРАУЗЕР: тебе РАЗРЕШЕНО открыть браузер, чтобы проверить СВОЮ только что сделанную
работу — без отдельного спроса. Открой лаб (или свою правку боевого) в чистом окне,
покликай, сними счётчик узлов реальными цифрами, приложи в отчёт.
ГЕЙТ 3 требует «видел своими глазами» — теперь это выполнимо, отговорки нет.
ГРАНИЦА: разрешение = «проверить СЕБЯ». Когда владелец ПОКАЗЫВАЕТ визуальный баг —
по-прежнему сперва читаешь CSS/код и предлагаешь план словами, браузер не открываешь.
```

---

## 🔒 ЖЁСТКАЯ КАРТА Э1 — ЧТО ПОРТИТСЯ / ЧТО УДАЛЯЕТСЯ (Эржан 2026-07-29)
Страх Эржана: «перенесёшь всё не нужное и испортишь сайт». ОТВЕТ: НЕТ. Портируются ТОЛЬКО фичи ниже,
с выбранным вариантом. Всё из «УДАЛИТЬ» — НЕ едет в боевой, сносится. Клод, читающий это: НЕ порти
ничего из «УДАЛИТЬ», даже если оно на каноне.

### ✅ ПОРТИТСЯ в боевой (реальные фичи, выбранный вариант)
lab-main (ЭТАЛОН/оболочка Home) · champion-card (СТРАНИЦА чемпа) · item-card · calc-app (главный калькулятор) ·
metahub · chat · profile · drafter (+мини-игра) · map (Карта+Страта) · patch (рельс) · tournaments.
+ веер из hover-reveal — переиспользуется ВНУТРИ metahub (не как отдельный лаб).

### 🔩 ЭТАЛОН / ИНФРА (не «портится как лаб», но informs)
lab-main = ЭТАЛОН канона и оболочки. lab-ui-kit = библиотека элементов (кнопки/иконки — источник).
lab-settings (2026-08-02) = ⚙ хаб юзер-настроек. Портируется НЕ как «раздел», а как **механизм**:
`wr-prefs.js` (корень репо) подключается блокирующим `<script>` в `<head>` боевого + ⚙-попап в шапке.
Даёт 3 настройки (сплэш-арт / акцент / плотность) через глобальные переменные, 0 пересозданных узлов.
Ждёт от владельца: (1) вариант вида хаба — список / сетка-карточки / компакт; (2) добро на перенос
4 токенов плотности (`--dens`, `--pad-blk`, `--gap-blk`, `--row-y`) в `canon-tokens.css`.

### 🗑 УДАЛЯЕТСЯ — НЕ ПОРТИТСЯ НИКОГДА
sidebar-views (рельс строится заново в каркасе Э1) · hover (заменён hover-reveal/каноном) ·
champ-picker (пусто) · glass (стекло уже выбрано) · design-system (была разведка канона; эталон теперь lab-main) ·
youtube (если не решим делать видео-фичу) · build (сборка ушла в калькулятор).

---

## ★★ ШАПКА ПРОМПТА — вставлять в НАЧАЛО каждого UI/порт-промпта (2026-08-08)
Собрано из уроков сессии (все 6 улучшений). Промпт = ШАПКА + конкретная задача. НЕ собирать ад-хок —
так проскочил баг «свой сплэш-арт» (промпт сказал «по канону», а не «линтер=0»). Копипаст целиком:

```
ПРОГОНИ 3 ГЕЙТА (CLAUDE.md), выведи Г1/Г3.
ЧИТАЙ ПЕРЕД КОДОМ: canon-tokens.css (единый источник ВСЕХ чисел) + DESIGN.md (канон) +
  <ЧЕРТЁЖ: SITE-SKELETON §X / LAB-CHOICES / Э1-PLAN / LAB-PARITY §X> + lab-main (ЭТАЛОН).
  Строй к СОГЛАСОВАННОМУ по чертежу, не из головы.
ВЛАДЕЛЕЦ ФАЙЛОВ: трогаешь ТОЛЬКО <перечисли файлы>. Чужие файлы/лабы НЕ трогай (два Клода на один файл = запрет).
ВСЁ через canon-tokens.css: 0 хардкода · акцент БЕЛЫЙ · выделение --sel-* · без эмодзи-иконок ·
  свой фон-арт ЗАПРЕЩЁН (только глобальный --splash-img).
НЕ ТАЩИТЬ ДЁРГАНЬЕ (блок выше) + lab-morph + точечный ре-рендер.
БРАУЗЕР ОБЯЗАТЕЛЕН: чистое/инкогнито окно. Приёмка ЦИФРОЙ (счётчик узлов N/N), не «на глаз».
★ ЛИНТЕР = ПРИЁМКА (жёсткий гейт, НЕ опция): node tools/canon-lint.mjs <твои файлы> = 0 находок.
  Не 0 → это НЕ «готово».
ФОРМАТ ОТЧЁТА: что сделал · линтер=0 · счётчик узлов N/N · что видел в браузере · что НЕ трогал · свои предложения.
```

## ★ ПРАВИЛА РАЗДАЧИ (для меня, Клода-советника)
- **ОДИН поток за раз**, не плодить параллель (17 чатов = хаос). Один Клод = один владелец области/файлов.
- **Промпт ЦЕЛИКОМ**, готовым к копипасту ([[feedback_prompts_whole]]). Правка = весь промпт заново.
- **Каждый промпт = ШАПКА + задача.** Не собирать по кускам — забуду гейт (линтер), и баг проскочит.

### ★ ПЕРВОЙ СТРОКОЙ В КАЖДЫЙ ПРОМПТ-ИСПОЛНИТЕЛЬ (2026-08-17)
```
ТЫ — ИСПОЛНИТЕЛЬ (worker): выполняешь КОНКРЕТНУЮ задачу-промпт → отчёт. Ты НЕ координатор/советник.
Идеи · план · «что дальше» · решения дизайна/архитектуры · законы — НЕ твоё, это ведёт Клод-советник.
Владелец прислал идею/план/вопрос стратегии → отвечай: «Ты пишешь не тому — я только исполняю задачи
и даю отчёт. Идеи/план/решения → к главному Клоду-советнику.» И НЕ отвечай за координатора.
```
*Почему:* владелец случайно вёл стратегию с worker-Клодом как с советником — тот наделал коорд-решений (⚠️).
Эта строка не даёт worker'у принять идею за команду себе.

### ★ ГЕЙТ ПОЛНОТЫ ЛАБА — в шапку порт-промптов (2026-08-20)
Перед портом вида worker СНАЧАЛА (в Гейт 1) сверяет ЧИСЛО блоков/фич лаба с LAB-PARITY §X.
Лаб неполный → недостающее взять из БОЕВОГО (адаптировать в канон), НЕ выдумывать. НЕ верить «готово» на слово.
*Почему:* метахаб — лаб-Клод заявил «11 блоков готово», по факту 10 (Сборка дня + лаунчер только в боевом).
Поверил без проверки — чуть не портанул неполный вид. Гейт ловит это до кода.

---

## 📋 ОТЛОЖЕНО / БЭКЛОГ Э1.x (durable — вместо «занёс в голову», 2026-08-20)
Улучшения/долги, которые всплыли по ходу и отложены. Писать СЮДА сразу, не в память-в-голове.

**Уборка Э1.9 (в конце, после видов):**
- Ширина `#stage` (пустые ~350px справа) — решить РАЗОМ для всех видов, не по одному.
- Переименовать классы строк Статс `.f-*` → эталонные `.ch-*` (унификация, ~200 строк CSS).
- Снос легаси: mMask (801 мёртвый узел), tierlistMask (мёртвая оболочка), userCardMask (пустой), старые .m-mask.

**Линтер:**
- git-diff режим для правила 11 (числа-мимо-токена): гейтить новый код, не шуметь 2086 легаси.

**Пайплайн/данные:**
- Мост «имя предмета/руны → slug» → клики в «Сборке дня» (сейчас 15% дали бы 404).
- Страницы для Skarner/Yunara (есть в снимке WR, нет страниц).
- Проверить патч-точки метахаба на реальных данных (patchMap был пуст).

**Отдельные треки:**
- Киберспорт: fix `firestore.rules` (чтение гостю) + `firebase deploy --only firestore:rules` (security).
- Турниры: геттер списка в cybersport.js → оживить блок «турниры» в метахабе (снять ДЕМО) + создать лаб Турниры.
- Кооп-драфт: скелет/подсветка при 6с ленивой загрузки (сейчас тишина).
- Доп-аудит боевого ПОД ЛОГИНОМ (чат/профиль/админ-модалки за гейтом — гость не видел).

**Проверить у Эржана (данные CMS):**
- Лента изменений: запись Irelia помечена buff, а текст — нерф. Качество данных, не код.
- Турниры-РЕГИСТРАЦИЯ через сайт (Эржан 2026-08-21): игроки/команды регистрируются на турнир НА сайте. Нужно: лаб Турниры (создать) + флоу регистрации + Firestore (лобби↔сетка↔заявки) + права. Обдумать как реализуем (office-hours). Отдельная СТРОЙКА, не порт.

### ★★ ПАРИТЕТ-ДЕЛЬТА — обязательный ПЕРВЫЙ шаг любого порта вида (Эржан 2026-08-21)
Перед кодом worker выводит ТАБЛИЦУ: всё, что есть в БОЕВОМ/старом, но чего НЕТ в лабе (пробелы паритета).
По КАЖДОМУ пробелу — ЖДЁТ решения советника (одно из):
  • СТРОИМ ЛАБ / достраиваем лаб (если фича важная и заслуживает своего дизайна);
  • АДАПТИРУЕМ из боевого в канон (логика есть, лаба нет — переодеть);
  • ВЫКИДЫВАЕМ (если реально лишнее — по решению Эржана).
НЕ порти, пока по всем пробелам нет решения. Найденные пробелы записывать в LAB-PARITY (карта сама бывает дырявой).
*Почему:* «нашли X, которого нет в лабе» — не сюрприз на каждом порте, а плановый шаг. Ничего не теряется,
и Эржан заранее решает, где нужен НОВЫЙ лаб (напр. Турниры-регистрация), а где хватит адаптации.
- CMS-данные (2026-08-21): (а) предмет с двумя РУ-именами — «Чёрная секира» в реестре vs «Чёрный разделитель» в карточке (один предмет, перевод разный); (б) руны разложены криво — 54 в «Основные», 1 во «Второстепенные». Качество данных, не код — проверить в админке.

### ★ ВОПРОСЫ — ТОЛЬКО ПЛОСКИМ ТЕКСТОМ (Эржан 2026-08-21)
Worker/любой Клод: НЕ использовать интерактивные вопросы-компоненты (radio-кнопки/AskUserQuestion).
Вопросы выводить ПЛОСКИМ ТЕКСТОМ: «Вопрос N: … · A) … · B) … · C) …». 
*Почему:* владелец пересылает ответы координатору — интерактивную кнопку он вынужден СКРИНШОТИТЬ, а это
жжёт токены и время. Плоский текст он копирует/пишет «1-А 2-А» одной строкой.

### ★ ВИЗУАЛЬНУЮ РАЗВИЛКУ — НЕ СПРАШИВАТЬ ТЕКСТОМ, ДЕЛАТЬ ТУМБЛЕРОМ (Эржан 2026-08-27)
Если развилка про ВИД/РАСКЛАДКУ (где живёт блок, как выглядит, какой из N видов) — НЕ задавать её вопросом
и НЕ выбирать самому. Собрать ОБА/ВСЕ варианта на переключателе в дизайн-полосе (механизм `*-min` сверху) —
Эржан щёлкнет и выберет живьём. После выбора лишние варианты выкинуть (дизайн-полоса на боевой не едет).
*Почему:* абстрактный текст «А или Б» Эржан не может оценить не видя; он дизайнит глазами, тумблером за секунду.
Техническое/данные (какой источник, статика сейчас/позже) — координатор решает сам, вопросом не грузить.
Связано: [[feedback_sandbox_variants]].
  → СТРОГО: турниры-регистрацию строить ИЗОЛИРОВАННО (свой лаб → свой раздел → свои Firestore-коллекции), НЕ вжимать в ядро/чужие файлы. Иначе риск сломать. Гейты + один владелец держат это.
- Единый пикер сайта (LAB-PARITY §10): свести ВСЕ пикеры (Статс-тоггл, тир-drag, Страта, матчапы, драфт) к одному общему ТАЙЛУ+данным — отдельный этап unification, НЕ внутри порта вида.
- Ширина Страты/доски: сейчас 512×512 (упирается в #stage). Решить — дать полную ширину окна (как драфтеру) или оставить. Связано с общей задачей ширины #stage (Э1.9).
