# COMPLAINTS.md — открытые проблемы + конвейер «жалоба → закон»

## ⚙️ КАК ЭТО РАБОТАЕТ (алгоритм)
Это НЕ архив жалоб, а конвейер. Поток такой:
**жалоба Эржана → чиним → как только закрыта, она становится ЗАКОНОМ** (в
CLAUDE.md или памятке-фидбэке) и **удаляется отсюда**. Поэтому файл не растёт:
здесь живёт только то, что ещё ОТКРЫТО. Закрытую жалобу тут не держим — она
уже закон.

## ⚠️ КОРНЕВОЙ ВЫВОД (читать первым)
Ошибки Клода — НЕ от незнания законов (все были в CLAUDE.md), а от того, что
закон НЕ сверяется в МОМЕНТ правки. Лекарство = ГЕЙТЫ (гейт видимости + чек-лист
«перед готово»), а не рост реестра.

## 🔴 ОТКРЫТО (доказано кодом)
- Турниры в метахабе захардкожены (`app.js:7350` `var tours=[…]`) → нужен
  авто-источник (связать с Киберспортом / скрейп Liquipedia). `[инет/субагент]`

## 👀 КЛОД ПРОВЕРЯЕТ САМ на чистом окне (вкус/лаг — кодом не докажешь; Эржану НЕ трогать)
- Перекрестье-ховер таблицы: ховер столбца по мыши НЕ реализован (есть только
  подсветка отсортированного столбца) — проверить лаг на пересечении
- «Крутизна» таблиц из main-lab — нужен ПОРТ макета, не только покраска стеклом
- Модалки/инструменты сайдбара = инлайн вкладки (система в коде есть — подтвердить)
- Тёмные модалки / свой сплэш-арт → один глобальный (вероятно уже закрыто)
- Шапка вкладок чуть скролится; рельс иконки прижаты/обрезаны

## ✅ ЗАКРЫТО за эту сверку (доказано кодом → ушли в законы, тут больше нет)
- `sidebarOpen is not defined` — определён глобально (`app.js:2917`). Был глюк кэша.
- Шапка таблицы была синяя — теперь `var(--bg-card)` = белое стекло (`styles.css:1797`).

_Правило ведения: новую жалобу — сюда; закрыл → удалить отсюда (она уже закон)._
_Последнее обновление: 2026-06-30._
