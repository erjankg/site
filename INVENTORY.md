# INVENTORY.md — аудит боевого перед Э1 (пересбор по канону)

**Тип:** read-only аудит. НИЧЕГО не удалено/не изменено — только карта.
**Дата:** 2026-07-10. **Ветка:** feat/damage-calc-boevoy.
**Легенда уверенности:** ✅ уверен (доказано кодом) · ⚠️ проверить (нужна ручная/браузерная сверка) · 💣 риск при удалении.

Связано: `MASTERPLAN.md` (этапы), `PORT-BACKLOG.md` (перенос лабов), `COMPLAINTS.md` (открытые баги), `DESIGN.md` (канон).

---

## 0. Как грузится боевой (критический старт)

`index.html` подключает по порядку:
1. `i18n.js` (в `<head>`) — словарь переводов RU/EN (817 строк).
2. Firebase 10.12.2 из CDN (app/auth/firestore/storage-compat) с SRI.
3. Инлайн-скрипт (`index.html:111`) — пре-флайт auth-гейта по localStorage (класс `cs-public-mode`/`pre-profile-gate`). Мелкий, ОК.
4. В конце `<body>`: Sortable + GSAP (CDN, SRI) → `share.js` → `cms.js` → `app.js` → `tab-pill.js` → `draft-logic.js` → `anim-perf.js` (defer).
5. Инлайн-скрипт (`index.html:1259`) — регистрация Service Worker + кнопка версии (только в PWA).

**Лениво (НЕ на старте), через `_lazyScript` в `app.js:157`:**
- `draft.js` (кооп-драфт) — стаб `openDraftCoop`, фоновая догрузка для залогиненных (`_bgLoadDraft`, `app.js:196`).
- `cybersport.js` — стабы `openCybersport`, `openCybersportTournament`.
- `layout-editor.js` — по клику «Редактор позиций» (админ) или если есть сохранённый лейаут.

✅ Ленивая загрузка тяжёлых фич сделана правильно (снят парсинг с критического пути).

---

## 1. ЧТО ЕСТЬ — фичи, разделы, JS-файлы

### 1.1 JS-файлы боевого (за что отвечает)

| Файл | Строк | Роль | На старте? |
|---|---|---|---|
| `app.js` | 7534 | Ядро: модалки, сайдбар, главные виды (Статс/WinRate/Метахаб/Тир), карточки чемпа/предмета/руны, чат, профиль, калькулятор-меню, тир-лист, инфлюенсеры | ✅ да |
| `styles.css` | 8171 | Все стили сайта (самый тяжёлый файл) | ✅ да |
| `cms.js` | 4109 | Загрузка данных из Firestore + вся админ-панель (предметы/руны/патчноуты/иконки/сайдбар/шрифты/лейаут/тексты/inline-edit) | ✅ да |
| `i18n.js` | 817 | Словарь переводов RU↔EN | ✅ да (head) |
| `share.js` | 364 | Генерация share-картинок (winrate/билд) через canvas | ✅ да |
| `tab-pill.js` | 95 | Анимированная «пилюля» под активной вкладкой | ✅ да |
| `draft-logic.js` | 99 | Чистые алгоритмы драфта (вынесены из draft.js, тестируются в `test/`) | ✅ да |
| `anim-perf.js` | 92 | Троттлинг анимаций/perf | ✅ да (defer) |
| `draft.js` | 3962 | Кооп-драфт (серии), UI+Firebase realtime | 🕓 лениво |
| `cybersport.js` | 1853 | Турнирная система (сетки, группы, плейофф), Firestore `/tournaments` | 🕓 лениво |
| `layout-editor.js` | 350 | Админ drag-редактор позиций → экспорт CSS | 🕓 лениво (админ) |

### 1.2 Главные виды (Home под-вкладки) — `switchMainView()` (`app.js:6626`)
- `main` = **Статс** (таблица матчапов) — дефолт.
- `wrpr` = **WinRate** (инлайн-секция `#wrprSection`, `index.html:217`). ✅ уже инлайн (не модалка).
- `hub` = **Метахаб** (веер-витрина из lab-hover-reveal, `app.js:7245`).
- `tier` = **Тир-лист**.
- `tactics` = **Тактич-доска** → ⚠️💣 `window.location.href='tactics-board/'` (`app.js:6637`) — **УХОДИТ на отдельную страницу**, ломает контракт верхних вкладок (`feedback_top_tabs_contract`: все виды инлайн). Э3 = пересобрать как инлайн «Страта».
- `patch` = **Патч** — плейсхолдер (PORT-BACKLOG: app.js:6639).
- Нет вида **Карта** (Э2, ещё не построен — заложить слот).

### 1.3 Сайдбар (`.side-btn`, `index.html:498+`) — инструменты рельса
`sideChamps` (чемпионы) · `calc` (калькулятор) · `items` (предметы) · `runes` (руны) · `drafterHub` (драфтер-хаб) · `changes` (admin, лента изменений) · `globalChat` (чат) · `cybersport` (киберспорт) · версия. Плюс admin-only `Categories` (`sideBtnCategories`).

### 1.4 Модалки — 24 в `index.html` (все зарегистрированы через `openModal`, кроме одной, см. §5)
`mMask` (чемпион-детали/пикер) · `calcMask` (калькулятор iframe) · `itemsMask` · `runesMask` · `tierlistMask` · `sideChampsMask` · `champDetailMask` · `runeDetailModal` · `itemDetailModal` · `itemSubModal` · `itemCalcMenuMask` · `drafterHubMask` · `draftMask` · `draftCoopMask` · `champPickerModal` · `tierlistMenuMask` · `changesMask` · `chatSystemMask` · `profileSetupMask` · `socialPickerMask` · `socialLinkConfirmMask` · `userCardMask` · `influencerMask` · `cybersportMask`.

### 1.5 Встроенные под-приложения (iframe / отдельная страница)
- **Калькулятор урона** = `calc-app/` (v34, `champ-base.js` + реальные статы), грузится в iframe `calc-app/index.html?embed=1` (`app.js:1527`). ✅ Живой, отдельный модуль.
- **Тактич-доска** = `tactics-board/` (`board.js` 418+, `style.css`, свой `layout-editor` через `LE_KEY_PREFIX='tb'`). Отдельная страница (не iframe).

### 1.6 SEO-статика (генерится `seo/generate.mjs`)
- `champions/` — **137** страниц · `items/` — **100** · `runes/` — **55** · `tier-list/` (+ per-role bot/jungle/mid/support/top) · одиночные `drafter/`, `coaching/`, `damage-calculator/`, `map-strategy/`. Итого ~300 статических лендингов. ✅ Не трогать (SEO), но и не грузятся в SPA.

### 1.7 Данные
`data-pipeline/` — `base-stats.json`, `wr-stats.json` (реальные, `fetch-wr-stats.mjs`), `abilities*.json`, `jungle-economy.json` (для Э2 Карта, много demo-чисел).

---

## 2. ДОХЛЫЙ КОД / СИРОТЫ / ДУБЛИ

### 2.1 Файлы-сироты (нигде не подключены в HTML/JS боевого) ✅
- ✅ **[УДАЛЕНО 2026-07-11]** `migrate.js` · `migrate-winrates.js` · `migrate-physical-items.js` · `migrate-translations.js` · `cleanup-user-emails.js` · `preview-design.html`. Пруф: 0 ссылок в index.html / любом .js / sw.js precache / package.json scripts / sitemap / SEO-HTML (единств. упоминания migrate — текст-описание в `.gstack/security-reports/…cso.json`; preview-design = мёртвый OG-мокап, реальное превью = `preview.jpg`).
- `.hover-lab-serve.mjs` — локальный дев-сервер для лаба, не для боевого. Оставлен (дев-утилита).

### 2.2 Лабы `lab-*/` (14 папок) — НЕ боевой
Песочницы (noindex). Статус переноса — в `PORT-BACKLOG.md`. Для Э1 не трогать; после переноса выбранных вариантов часть можно архивировать.
- ✅ **[УДАЛЕНО 2026-07-11]** `lab-damage-calc/` (stale-копия v18). Пруф: боевой iframe грузит `calc-app/index.html` (app.js:1527); единств. упоминание lab-damage-calc = комментарий-след index.html:305.
- ✅ [ЗАКРЫТО 2026-07-10] Демо-текст email в `lab-main/main-lab.js:337` заменён на `user@example.com`.

### 2.3 Дубли/легаси-механизмы
- **Два «редактора лейаута»** (разные вещи, не дубль, но оба админ-only, оба кандидаты в §4):
  - `cmsOpenLayoutEditor` (`cms.js:1454`) = «⚙ Настройки лейаута» (числовой конфиг в Firestore `_cmsSiteConfig`).
  - `layout-editor.js` / `_openLayoutEditor` = «🎚 Редактор позиций» (drag → экспорт CSS, дев-инструмент авторинга).
- **Легаси-список имён стекла** в `:is(.lvl-container, .m-win, .hub-card …)` — по CLAUDE.md это доканоническое поимённое подключение стекла; новое только через `.glass`. При Э1 переводить блоки на `.glass`, список не расширять.
- **Мёртвая ветка HTML:** `index.html:1022` `<!-- wrprMask removed -->` — WinRate стал инлайн, старая модалка удалена (комментарий-след, можно снять).

### 2.4 Дохлый CSS
⚠️ Требует инструментального анализа (styles.css = 8171 строк). Точечно НЕ проверял неиспользуемые классы — рекомендация для Э1: прогнать PurgeCSS/coverage в браузере ПОСЛЕ пересбора разметки, т.к. много классов создаётся динамически в JS (`classList.add`) и статический анализ даст ложные срабатывания.

---

## 3. ВЕС / ОПТИМИЗАЦИЯ

**Самые тяжёлые (байт):** `app.js` 440 КБ · `styles.css` 310 КБ · `index.html` 222 КБ · `draft.js` 196 КБ · `cms.js` 188 КБ.

Кандидаты / наблюдения:
- ✅ `draft.js` и `cybersport.js` уже сняты с критического старта (lazy). ХОРОШО.
- ⚠️ **SW precache тянет `draft.js` + `cybersport.js`** (`sw.js:28,32`) на первом визите — это отменяет часть выгоды lazy (парсинг отложен, но ~380 КБ всё равно качаются на первом визите). Для Э1 рассмотреть исключение их из PRECACHE (грузить runtime-cache по требованию).
- ⚠️ `index.html` 222 КБ — почти весь вес = 24 инлайн-модалки в разметке. По канону DESIGN.md модалок нет → единая обёртка + **ленивые разделы**. Э1 должен резко ужать HTML (рендерить разделы по требованию, а не держать все 24 в DOM).
- ⚠️ `styles.css` 310 КБ единым файлом грузится на старте — после Э1-пересбора кандидат на разбиение (критический CSS + ленивые секции).
- ⚠️ `cms.js` 188 КБ грузится на старте, но нужен в основном АДМИНУ (редакторы) + первичная загрузка данных. Разделить: `cms-data.js` (загрузка данных, всем) + `cms-admin.js` (редакторы, лениво только админу) — крупный выигрыш для 99% посетителей.
- `i18n.js` 53 КБ на старте — норм (нужен сразу для перевода).

---

## 4. ЮЗЛЕС-АДМИНКА (меню «👤», `index.html:183-189`) — что можно снести

Все пункты `admin-only` (скрыты для обычных юзеров, `display:none` + гейт по `_isAdmin`). Загружаются в `cms.js` (кроме layout-editor.js — ленивый). Для обычного посетителя = мёртвый вес только в `cms.js`.

| Пункт меню | Функция / файл | Что делает | Вердикт |
|---|---|---|---|
| 📋 История изменений | `cmsOpenChangelog` (cms.js:656) | Просмотр лога правок из Firestore `changelog` | Оставить (аудит), лёгкий |
| ⚙ Настройки лейаута | `cmsOpenLayoutEditor` (cms.js:1454) | Числовой конфиг раскладки → Firestore | 💣 Зависит: `_cmsSiteConfig` применяется на боевом. Снести только вместе с пересбором раскладки в Э1 |
| 🎚 Редактор позиций | `_openLayoutEditor` → `layout-editor.js` | Drag-редактор → экспорт CSS (дев-инструмент авторинга) | **Кандидат на снос** — это дев-тулза для подбора CSS, не пользовательская. После Э1-канона не нужна. Ленивая → на юзеров не влияет |
| 🧹 Очистить все патч-ноты | `cmsClearAllPatchnotes` (cms.js:1217) | 💣 МАССОВОЕ удаление всех патч-нотов из Firestore (двойное подтверждение есть) | Оставить (рабочий инструмент), но опасный — держать за двойным гейтом |
| 🖼 Иконки | `cmsOpenIconsEditor` (cms.js:2021) | Редактор иконок сайта | Оставить (контент-инструмент) |
| 🔤 Сайдбар | `cmsOpenSidebarEditor` (cms.js:2178) | Переименование кнопок сайдбара | ⚠️ После Э1 (навигация Suno, рельс из DESIGN.md) — может стать неактуальным |
| 🔤 Шрифты сайта | `cmsOpenFontEditor` (cms.js:3752) | Смена шрифтов сайта | ⚠️ Канон DESIGN.md фиксирует шрифты лаба → редактор шрифтов противоречит «одна правда». Кандидат на снос в Э6 |

**Зависимости (что сломается при сносе):**
- Снос `layout-editor.js` + «Редактор позиций»/«Настройки лейаута» → могут отвалиться сохранённые CSS-оверрайды (`localStorage le_layout_pc/mobile`, `_cmsSiteConfig`). Перед сносом убедиться, что все нужные правки уже зашиты в `styles.css`.
- «Шрифты»/«Сайдбар» пишут в `_cmsSiteConfig` и применяются при загрузке (`cmsLoadSiteConfig`, `cmsApplyAllSiteTexts`). Снос редактора без сноса применения = мёртвая конфа, но не поломка.
- **Общий принцип:** редакторы = ТОЛЬКО UI; данные живут в Firestore/localStorage и применяются отдельными `cmsLoad*`/`cmsApply*`. Снести можно редактор (UI), оставив применение — безопасно.

---

## 5. КОЛЛИЗИИ

- ⚠️💣 **`drafterHubMask` открывается через `openModal`, но НЕ в `MODAL_IDS`** (`app.js:47-49`; открытие `app.js:3051`). Как main-модалка кладётся в `_modalStack`, но `_applyStackVisuals` перебирает только `MODAL_IDS` при скрытии → при открытии ДРУГОЙ модалки поверх `drafterHubMask` его `.active` может не сняться (латентный баг «две модалки видны»). **Проверить в браузере; добавить id в MODAL_IDS при Э1.**
- ✅ **Дублей `id=` в `index.html` НЕТ** (проверено `uniq -d`).
- ✅ **`openModal` определён единожды** (`app.js:242` `window.openModal`); `cybersport.js`/`draft.js` его только вызывают. Ранее найденный «глобальный конфликт openModal» = корректная единая точка. `lab-settings.js:103` имеет свою локальную `openModal(mode,text)` — но это ЛАБ, другой файл/скоуп, на боевой не грузится → не коллизия.
- ⚠️ **Множество shared-globals `window._cms*` / `window._lang` / `window.patchMap`** пишутся и в `app.js`, и в `cms.js` (перекрёстная связность по window.*). Не баг сам по себе, но хрупко: порядок инициализации важен. Задокументировать «владельца» каждого при Э1.
- ⚠️ **app.js = 5+ изолированных IIFE-коробок** (границы: 5–3398, 3405–6929, 6935–7042, 7051–7158, 7166–7228, 7228+). Память `project_appjs_iife_scopes` УСТАРЕЛА (говорит про 3 коробки/другие номера строк) — обновить. Приватные `var` одной коробки не видны в другой = корень cross-scope ReferenceError (экспонировать через `window.*`).

---

## 6. СЕКРЕТЫ

- ✅ **[ЗАКРЫТО 2026-07-10] Утечка email в боевом:** `cms.js:1887` — параметр `&de=<email>` в URL переводчика MyMemory убран (email больше НЕ грузится на боевом). Дубль в `lab-main/main-lab.js:337` (демо, лаб) заменён на `user@example.com`. Проверка админа — по UID (`ADMIN_UID`) + Firestore `isAdmin`, не по email; настоящая защита — `firestore.rules`.
- ⚠️ **Firebase config** (`app.js:3486`, `apiKey: "AIzaSy…"`, projectId `wildrift-stats-600c0` и т.д.) — по дизайну Firebase Web API-ключ ПУБЛИЧНЫЙ (не секрет), защита = Firestore Rules. Это норма, НЕ утечка. Просто зафиксировать, что безопасность держится на `firestore.rules`.
- ✅ Прочих секретов (Bearer/privateKey/пароли/AIza вне firebase) — не найдено. `token` в коде = share-токены драфта/URL-параметры (не секреты).
- ✅ CSP присутствует (`index.html:9`), но `script-src` содержит `'unsafe-inline'` (из-за inline `onclick=`). TODO в коде честно помечен (`index.html:8`). Соответствует `security_audit` MEDIUM #4 (открыт).

---

## 7. КАРТА для Э1 — оставить / переделать в канон / удалить

| Фича / артефакт | Решение | Примечание |
|---|---|---|
| Статс (таблица матчапов) | 🔄 переодеть в канон | Логику/данные не трогать (MASTERPLAN Э1) |
| WinRate (`#wrprSection`) | 🔄 канон | Уже инлайн — хороший образец |
| Метахаб (веер hub) | 🔄 канон | Веер god-tier (`feedback_keep_godtier`) — сохранить 1-в-1 |
| Тир-лист | 🔄 канон + сделать инлайн | Сейчас модалка/фуллскрин |
| Тактич-доска (`tactics-board/`) | 🔄 пересобрать как «Страта» инлайн (Э3) | Сейчас уходит на отд. страницу — ломает контракт вкладок |
| Патч (вид) | 🔄 доделать (сейчас плейсхолдер) | В рельс по MASTERPLAN |
| Карта (инфо/экономика) | ➕ построить (Э2) | Данные `jungle-economy.json`; заложить слот в Э1 |
| Драфтер solo | 🔄 канон | Портнут (app.js:3060) |
| Кооп-драфт (`draft.js`) | ✅ оставить как «клетка» фуллскрин | Исключение из инлайн-правила |
| Калькулятор (`calc-app/`) | 🔄 пересобрать в лабе на каноне (Э6) | Отдельный iframe-модуль |
| Предметы / Руны / Чемпионы (сайдбар) | 🔄 инлайн-вкладки (канон) | lab-sidebar-views образец |
| Чат | 🔄 канон | Карточки-ссылки портнуты (app.js:5036) |
| Киберспорт (`cybersport.js`) | 🔄 канон, оставить lazy | Турниры в метахабе захардкожены (COMPLAINTS открыт) |
| 24 инлайн-модалки в index.html | 🔄 → единая обёртка + ленивые разделы | Резко ужать HTML (222 КБ) |
| Админ: Редактор позиций / Шрифты / Сайдбар | 🗑 снести в Э6 | Дев/устаревшие инструменты; проверить зависимости §4 |
| Админ: Очистить патчноуты / Иконки / История / Настройки лейаута | ✅ оставить | Рабочие контент-инструменты |
| migrate*.js, cleanup-user-emails.js | 🗑 архив | Сироты, одноразовые |
| lab-damage-calc/ (stale v18) | 🗑 архив | Боевой = calc-app v34 |
| preview-design.html | ⚠️→🗑 | Проверить, вероятно мёртв |
| email в cms.js:1887 | 🗑 убрать немедленно | Живая утечка §6 |
| cms.js разбить (data/admin) | 🔄 оптимизация | Снять админ-вес с обычных юзеров |
| SW precache draft/cybersport | 🔄 убрать из precache | §3 |

---

## Итог одной строкой
Боевой = 1 монолит `app.js` (5+ IIFE-коробок) + `cms.js` (данные+админка) + ленивые `draft.js`/`cybersport.js` + iframe-калькулятор + отд. страница тактич-доски + ~300 SEO-лендингов. Главные долги перед Э1: **живая email-утечка (cms.js:1887)**, **тактич-доска уходит со страницы (ломает контракт вкладок)**, **drafterHubMask вне MODAL_IDS**, **222 КБ index.html из 24 модалок → ленивые разделы**, **cms.js весь на старте → разделить**, устаревшая память про IIFE-скоупы.
