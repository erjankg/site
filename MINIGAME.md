# MINIGAME.md — чертёж драфт-мини-игры (Wild Rift Stats)

Чертёж ДО стройки. Тесно связан с: `DRAFT-AI.md` (движок знаний/матчапы), `draft.js`/`draft-logic.js`
(существующий драфтер), профиль (`lab-profile`), `champion-tags.json`, `guides/*.json` (winrate).
Память: `project_draft_minigame`. Не строим, пока не согласован.

## 1. Что это и зачем
Драфт-игра с рейтингом и лидербордом. **Игра = драфт** (пик/бан), победителя определяет **ИИ-судья**.
**Зачем это сильно:** победы игрока у нас = ЧЕСТНАЯ статистика (он сыграл НА сайте) → закрывает дыру
профиля «нет match API». Плюс реиграбельность + соревнование = удержание.

## 2. ОДИН глобальный драфтер, режимы через ТУМБЛЕРЫ
Не отдельный поток — тумблеры поверх существующего драфтера:
- **ИИ-судья** вкл/выкл · **ИИ-помощник** (подсказки пиков) вкл/выкл.
- **Судья считает ВСЕГДА** (при «выкл» просто скрыт от капитанов/зрителей во время драфта) — нужен для
  статистики и лидерборда.
- **ГРАНИЦА:** кооп-«клетка» (реальные 2 игрока, фуллскрин, `data-no-esc`) НЕ переписывается. Режимы = слой поверх.

## 3. ИИ-судья — как считает (прозрачная ЭВРИСТИКА, не ML)
Вход — то, что уже построено:
- **6 функций состава** из `champion-tags.json.funcs` (frontline/engage/carry/waveclear/peel/lean AD-AP).
- **Матчапы** (3 слоя из DRAFT-AI.md): статистика winrate → редакционные контры → «почему» из тегов.
- **Мета/тир** из `wr-stats.json` (винрейт/пик/бан по рангам).
- **Юзерские тир-листы** (Firestore) — приоритет над авто (решение приоритета за Эржаном).

`Score(team) = Σ(закрытые функции × вес) + Σ(преимущества контрпика) + баланс AD/AP + синергии − штрафы за дыры`
`Вероятность = softmax(ScoreA, ScoreB)`.
**ВСЕГДА показывает «почему»** (какие функции закрыты, какие контры сыграли, чей тир выше).
Никогда не выдавать за нейросеть — это честная эвристика с разбивкой.

## 4. ИИ-помощник (тумблер)
Подсказывает пик из ТОГО ЖЕ движка (закрой дыру состава / возьми контру / забери мету).
Один движок знаний на судью И помощника — не два (закон «одна вещь в одном месте»).

## 5. Рейтинг + лидерборд
Очки (черновик Эржана): победа карты **+1** · серия 2-0 **+3** · 2-1 **+2** · проигравшему −1 карта ·
−2 за серию 2-1 · −3 за 2-0. Хранить в Firestore per-user (ELO/очки) → лидерборд.
**Связь с профилем:** победы/рейтинг = честная статистика игрока (то, чего не хватало профилю).

## 6. Режимы (открыто — выбрать)
Кандидаты: соло-vs-ИИ · рейтинговый 1v1 · кооп-серии (существующая клетка) · «угадай про-матч».
Все через тумблеры одного драфтера. Список финализирует Эржан.

## 7. Что переиспользуем / конфликты
- **Расширяем `draft.js`/`draft-logic.js`** (там уже пик/бан/ходы/стороны/таймер/транзакции) — НЕ строим заново.
- **Движок знаний ОДИН** — общий для матчапов, судьи, помощника.
- **Конфликт-риск:** клетку кооп-драфта не трогать; рейтинг/судья — слой поверх, не переписывание.
- Данные готовы: `champion-tags.json`, `guides/*.json` (winrate), `counters.json`, `wr-stats.json`.

## 8. Этапы (форвард)
- **М1:** ИИ-судья на готовых данных (funcs+матчапы+тир) + «почему». Тумблер вкл/выкл.
- **М2:** рейтинг+лидерборд (Firestore) → связь с профилем.
- **М3:** ИИ-помощник (подсказки пиков).
- **М4:** режимы (соло-vs-ИИ, рейтинговый) + полировка.
(Всё после Э1 — сначала боевой на каноне; но крючки в профиле/тир-листе закладываем заранее.)

## 9. Открытые вопросы (решает Эржан)
1. Список режимов (п.6).
2. Приоритет тир-листа: юзерский перебивает авто-ИИ?
3. Детали рейтинга (ELO-формула, сезоны/сброс?).
4. Строим после Э1 (рекомендация) или параллельно?
